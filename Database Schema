CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE,
    password VARCHAR(255)
);

CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255),
    description TEXT,
    price DECIMAL(10,2),
    image VARCHAR(255)
);

CREATE TABLE orders (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    total DECIMAL(10,2),
    status VARCHAR(50) DEFAULT 'Pending'
);

-- Sample products
INSERT INTO products (name, description, price, image) VALUES
('Lavender Serenity', 'Calming soy candle', 649, 'https://example.com/lavender.jpg'),
('Vanilla Warmth', 'Rich vanilla beeswax', 599, 'https://example.com/vanilla.jpg');